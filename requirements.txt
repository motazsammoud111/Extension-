# Digital Twin AI — Dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────
openai>=1.30.0                  # SDK OpenAI — utilisé pour Groq (compatible)
# anthropic>=0.25.0             # Claude API — réactiver quand on passe à Claude
fastapi>=0.111.0                # API REST
uvicorn[standard]>=0.29.0       # ASGI server
pydantic>=2.7.0                 # Data validation
python-dotenv>=1.0.1            # .env loading
httpx>=0.27.0                   # HTTP async client

# ── Data & Storage ────────────────────────────────────────────
# SQLite is built-in — no extra package needed
supabase>=2.4.0                 # Supabase Python SDK (prod DB cloud)
# asyncpg>=0.29.0              # Pour PostgreSQL direct (optionnel plus tard)
# sqlalchemy[asyncio]>=2.0.0

# ── NLP & Analysis ────────────────────────────────────────────
# Built-in (re, collections, json) — no extra needed for MVP
# For advanced NLP (optional):
# transformers>=4.40.0
# sentence-transformers>=2.7.0   # Pour le RAG sémantique
# faiss-cpu>=1.8.0               # Vector search

# ── Telegram Integration ──────────────────────────────────────
python-telegram-bot>=21.3       # Telegram Bot API

# ── WhatsApp & Instagram ──────────────────────────────────────
# Uses httpx (already listed above) + Meta Graph API
# No additional SDK required

# ── Frontend (optionnel — si tu build le frontend en Python) ──
# streamlit>=1.35.0             # Dashboard rapide (optionnel)

# ── Development ───────────────────────────────────────────────
python-multipart>=0.0.9         # File uploads dans FastAPI
watchdog>=4.0.0                 # Surveillance de fichiers (fine_tune watch)
